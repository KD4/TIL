Http Basic 인증
=============================

Basic HTTP 인증 스킴은 base64를 이용하여 인코딩된 사용자 ID/비밀번호 쌍의 인증 정보를 전달한다.

사용자 ID와 비밀번호가 거의 평문으로 네트워크를 통해 전달되기 때문에 (base64 인코딩은 복호화 가능하다.), Basic 인증 스킴은 안전하지 않다. 때문에 HTTPS / TLS와 같이 사용되어야한다. 

이런 추가적인 보안성 향상이 없다면 사용하는 것을 지양해야한다.

